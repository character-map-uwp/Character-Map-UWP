<Page
    x:Class="CharacterMap.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CharacterMap"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:CharacterMap.Core"
    xmlns:controls1="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core1="using:Microsoft.Xaml.Interactions.Core"
    Loaded="MainPage_Loaded"
    DataContext="{Binding Source={StaticResource Locator}, Path=Main}"
    mc:Ignorable="d">

    <Page.Resources>
        <core:AppSettings x:Key="AppSettings" />
        <CollectionViewSource x:Name="GroupedFontList" x:Key="GroupedFontList" IsSourceGrouped="True" Source="{Binding GroupedFontList}"/>
    </Page.Resources>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" KeyDown="LayoutRoot_KeyDown" KeyUp="LayoutRoot_KeyUp">
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
            <!--<RowDefinition Height="Auto" />-->
        </Grid.RowDefinitions>
        <!-- Title Bar Grid-->
        <Grid Grid.Row="0" Height="{x:Bind CoreTitleBarHeight, Mode=OneWay}" Canvas.ZIndex="3" RequestedTheme="Dark">
            <Rectangle x:Name="TitleBarBackgroundElement" Fill="{StaticResource SystemControlBackgroundAccentBrush}" />
            <Grid Padding="{x:Bind CoreTitleBarPadding, Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Text="Character Map" Margin="10,5" HorizontalAlignment="Stretch" FontSize="13" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ToggleSwitch RequestedTheme="Dark" Style="{StaticResource NiceToggleSwitchStyle}"
                                  Margin="0,-5,0,0"
                                  FontSize="13"
                                  VerticalAlignment="Top"
                                  HorizontalAlignment="Right"
                                  HorizontalContentAlignment="Right"
                                  x:Uid="Settings_ThemeToggle" OnContent="Light" OffContent="Dark"
                                  IsOn="{x:Bind ViewModel.IsLightThemeEnabled, Mode=OneWay}">
                        <interactivity:Interaction.Behaviors>
                            <core1:EventTriggerBehavior EventName="Toggled">
                                <core1:InvokeCommandAction Command="{x:Bind ViewModel.SwitchThemeCommand}"/>
                            </core1:EventTriggerBehavior>
                        </interactivity:Interaction.Behaviors>
                    </ToggleSwitch>

                    <Button x:Name="BtnSettings" Style="{StaticResource IconButtonStyle}">
                        <interactivity:Interaction.Behaviors>
                            <core1:EventTriggerBehavior EventName="Click" SourceObject="{Binding ElementName=BtnSettings}">
                                <core1:NavigateToPageAction TargetPage="CharacterMap.Views.SettingsPage" />
                            </core1:EventTriggerBehavior>
                        </interactivity:Interaction.Behaviors>
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE115;" FontSize="15" />
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
        <!-- Main UI Grid -->
        <Grid Grid.Row="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="265" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <Storyboard x:Name="BorderFadeInStoryboard">
                    <DoubleAnimation 
                        From="0" 
                        To="1" Storyboard.TargetName="BorderCopiedMessage" Storyboard.TargetProperty="Opacity" Duration="0:0:0.6" />
                </Storyboard>

                <Storyboard x:Name="BorderFadeOutStoryboard">
                    <DoubleAnimation 
                        From="1" 
                        To="0" Storyboard.TargetName="BorderCopiedMessage" Storyboard.TargetProperty="Opacity" Duration="0:0:0.6" />
                </Storyboard>
            </Grid.Resources>

            <!-- Font List Grid -->
            <Grid Grid.Column="0" Background="{ThemeResource SystemControlPageBackgroundListLowBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <ToggleSwitch 
                    Grid.Row="0"
                    Margin="10,5,0,5"
                    RequestedTheme="Dark"
                    x:Name="ToggleSymbolFontsOnly"
                    VerticalAlignment="Bottom"
                    IsOn="{Binding ShowSymbolFontsOnly, Mode=TwoWay}"
                    x:Uid="ToggleSymbolFontsOnly" Style="{StaticResource NiceToggleSwitchStyle}" />
                </Grid>

                <Grid Grid.Row="1">
                    <SemanticZoom>
                        <SemanticZoom.ZoomedInView>
                            <ListView x:Name="LstFontFamily" 
                          ItemsSource="{Binding Source={StaticResource GroupedFontList}}" 
                          SelectedItem="{Binding SelectedFont, Mode=TwoWay}" 
                          SelectionMode="Single"
                          SelectedValuePath="Name"
                          Margin="0,0,0,0">

                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListViewItem">
                                                    <ListViewItemPresenter CheckBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}" 
                                                   ContentMargin="{TemplateBinding Padding}" 
                                                   CheckMode="Inline" 
                                                   ContentTransitions="{TemplateBinding ContentTransitions}" 
                                                   CheckBoxBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}" 
                                                   DragForeground="{ThemeResource ListViewItemDragForegroundThemeBrush}" 
                                                   DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}" 
                                                   DragBackground="{ThemeResource ListViewItemDragBackgroundThemeBrush}" 
                                                   DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}" 
                                                   FocusBorderBrush="{ThemeResource SystemControlForegroundAltHighBrush}" 
                                                   FocusSecondaryBorderBrush="{ThemeResource SystemControlForegroundBaseHighBrush}" 
                                                   HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                   PointerOverForeground="{ThemeResource SystemControlHighlightAltBaseHighBrush}"
                                                   PressedBackground="{ThemeResource SystemControlHighlightListMediumBrush}"
                                                   PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}"
                                                   PointerOverBackground="{ThemeResource SystemControlHighlightListLowBrush}"
                                                   ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}" 
                                                   SelectedPressedBackground="{ThemeResource SystemControlHighlightListAccentHighBrush}"
                                                   SelectionCheckMarkVisualEnabled="True" 
                                                   SelectedForeground="{ThemeResource SystemControlForegroundAccentBrush}"
                                                   SelectedPointerOverBackground="{ThemeResource SystemControlHighlightListAccentMediumBrush}" 
                                                   SelectedBackground="{ThemeResource SystemControlBackgroundAltHighBrush}"
                                                   VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListView.ItemContainerStyle>

                                <ListView.Resources>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.Resources>

                                <ListView.GroupStyle>
                                    <GroupStyle HidesIfEmpty="True">
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}" BorderThickness="2" Height="45" Width="45" Margin="0,0,0,0" HorizontalAlignment="Stretch">
                                                    <TextBlock Text="{Binding Key}" Foreground="{ThemeResource SystemControlBackgroundAccentBrush}" FontSize="31" Padding="6,0,0,0" FontWeight="SemiLight" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                    </GroupStyle>
                                </ListView.GroupStyle>

                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Border BorderThickness="2" BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}" Height="36" />
                                            <TextBlock Text="{Binding Name}" Padding="8,0,0,0" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </SemanticZoom.ZoomedInView>

                        <SemanticZoom.ZoomedOutView>
                            <GridView x:Name="ZoomGridView" ItemsSource="{Binding CollectionGroups, Source={StaticResource GroupedFontList}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Border Margin="2" 
                                        Width="60" 
                                        Height="60" 
                                        HorizontalAlignment="Center" 
                                        Background="{Binding Group.Count,Converter={StaticResource ZoomBackgroundConverter}}">
                                                <TextBlock Text="{Binding Group.Key}" 
                                               Foreground="White" 
                                               FontSize="33" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                            </Border>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </SemanticZoom.ZoomedOutView>
                    </SemanticZoom>
                </Grid>

                <Border Grid.Row="2" Height="1" Background="{ThemeResource AppBarSeparatorForegroundThemeBrush}" Margin="0,10,0,0" />
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Selected Font Title Grid -->
                <Grid Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Height="50">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="7*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding ElementName=LstFontFamily, Path=SelectedValue}" FontSize="23" FontWeight="Light" Foreground="White" Margin="10,8,10,10" />
                        <TextBlock Grid.Column="1" Text="{Binding Chars.Count}" FontWeight="Light" Foreground="White" VerticalAlignment="Center" />
                        <Button x:Name="BtnSetDefault" Grid.Column="2" Content="Set As Default" RequestedTheme="Dark" HorizontalAlignment="Right" Click="BtnSetDefault_OnClick" />
                    </Grid>

                    <SearchBox Grid.Column="1" x:Name="SearchBoxUnicode" 
                       PlaceholderText="Unicode" 
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Bottom"
                       BorderThickness="0"
                       Margin="10"
                       SearchHistoryEnabled="True" 
                       QuerySubmitted="SearchBoxUnicode_OnQuerySubmitted" />
                </Grid>

                <!-- Char Browsing Grid -->
                <Grid Grid.Row="1" Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="7*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <GridView x:Name="CharGrid" 
                          ItemsSource="{Binding Chars}" 
                          SelectionMode="Single"
                          SelectedItem="{Binding SelectedChar, Mode=TwoWay}"
                          Grid.Column="0"
                          Margin="10,0,0,0"
                          Padding="0,0,4,4">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="1" 
                                        BorderBrush="{ThemeResource SystemControlDisabledChromeHighBrush}" 
                                        Height="{Binding Source={StaticResource AppSettings}, Path=GridSize}" 
                                        Width="{Binding Source={StaticResource AppSettings}, Path=GridSize}">
                                    <Grid>
                                        <TextBlock FontFamily="{Binding ElementName=LstFontFamily, Path=SelectedValue}" 
                                           Text="{Binding Char}"
                                           FontSize="{Binding Source={StaticResource AppSettings}, Path=CharPreviewFontSize}" 
                                               VerticalAlignment="Center"
                                               Margin="0,0,0,0"
                                               HorizontalAlignment="Center" 
                                               TextAlignment="Center" />

                                        <StackPanel Margin="2" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding Path=UnicodeString}" 
                                                   FontFamily="Consolas" 
                                                   FontSize="12" 
                                                   TextAlignment="Center" 
                                                   Foreground="#888" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>

                    <Border x:Name="PreviewPanel" Grid.Column="1" Margin="10,0,10,0" 
                    Padding="10"
                    BorderThickness="1,0,0,0" 
                    BorderBrush="{ThemeResource SystemControlForegroundChromeDisabledLowBrush}">
                        <Grid VerticalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Viewbox Height="Auto" Width="Auto" Grid.Row="0" Stretch="Uniform" StretchDirection="Both">
                                <TextBlock x:Name="TxtPreview" 
                                           TextAlignment="Center"
                                           Text="{Binding SelectedChar.Char}"
                                           FontFamily="{Binding ElementName=LstFontFamily, Path=SelectedValue}" />
                            </Viewbox>

                            <Border x:Name="BorderCopiedMessage" 
                                Grid.Row="0"
                                Background="{ThemeResource SystemControlBackgroundAccentBrush}" 
                                Opacity="0"
                                Margin="10,10,10,50" Padding="16,7" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                                <TextBlock x:Name="TxtCopiedMessage" x:Uid="TxtCopiedMessage" Foreground="White" />
                            </Border>

                            <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>

                                    <Button Click="BtnCopy_OnClick" HorizontalAlignment="Stretch" Content="Copy (Text Only)" />

                                    <Button x:Name="BtnSavePng" Grid.Column="2" Content="#BtnSavePng" x:Uid="BtnSavePng" HorizontalAlignment="Stretch" Command="{Binding CommandSavePng}" />
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0" Margin="0,5,0,0" x:Name="TxtXamlCode" Header="#TxtXamlCode" x:Uid="TxtXamlCode" GotFocus="TxtXamlCode_OnGotFocus" IsReadOnly="True" Text="{Binding XamlCode}" />
                                    <Button Grid.Column="2" x:Name="BtnCopyXamlCode" Click="BtnCopyXamlCode_OnClick" VerticalAlignment="Bottom">
                                        <Button.Content>
                                            <SymbolIcon Symbol="Copy" />
                                        </Button.Content>
                                    </Button>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0" Margin="0,5,0,0" x:Name="TxtFontIcon" Header="#TxtFontIcon" x:Uid="TxtFontIcon" GotFocus="TxtFontIcon_OnGotFocus" IsReadOnly="True" Text="{Binding FontIcon}"/>
                                    <Button Grid.Column="2" x:Name="BtnCopyFontIcon" Click="BtnCopyFontIcon_OnClick" VerticalAlignment="Bottom">
                                        <Button.Content>
                                            <SymbolIcon Symbol="Copy" />
                                        </Button.Content>
                                    </Button>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0" Margin="0,5,0,0" x:Name="TxtSymbolIcon" Header="Symbol Icon" x:Uid="TxtSymbolIcon" GotFocus="TxtSymbolIcon_OnGotFocus" IsReadOnly="True" Text="{Binding SymbolIcon}"/>
                                    <Button Grid.Column="2" x:Name="BtnCopySymbolIcon" Click="BtnCopySymbolIcon_OnClick" VerticalAlignment="Bottom">
                                        <Button.Content>
                                            <SymbolIcon Symbol="Copy" />
                                        </Button.Content>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!--Column Grid Splitter-->
                    <controls1:GridSplitter
                                Width="11"
                                Background="{StaticResource SystemControlBackgroundBaseLowBrush}"
                                GripperCursor="Default"
                                HorizontalAlignment="Left"
                                Grid.Column="1"
                                ResizeDirection="Auto"
                                ResizeBehavior="BasedOnAlignment"
                                CursorBehavior="ChangeOnSplitterHover"
                                GripperForeground="White">
                    </controls1:GridSplitter>

                </Grid>
            </Grid>
        </Grid>
        <!--<Grid Grid.Row="2" Margin="10" BorderThickness="0,1,0,0" BorderBrush="#CECECE">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSelectedText" 
                     PlaceholderText="Select a character to put it here." 
                     FontFamily="{Binding ElementName=LstFontFamily, Path=SelectedValue}" />
            <Button x:Name="BtnCopySelectedText" Grid.Column="1" Content="Copy (Rich Format)" Margin="10,0,0,0" />
        </Grid>-->
    </Grid>
</Page>